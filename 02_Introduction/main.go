package main

// This is an import block,
// it allows multiple packages to be imported without
// repeating the "import" keyword
import (
	"fmt" // imported and must be used
)

/*
	The main function,
	part of the main package,
	identifies the entry point of an application
*/
func main() {
	fmt.Println("Hello, playground")
}
